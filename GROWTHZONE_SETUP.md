# GrowthZone OAuth Setup (Legacy)

> **Deprecated.** Primary auth is now email OTP via ChamberMaster-backed Supabase functions.
> See [`MEMBER_AUTH_SETUP.md`](MEMBER_AUTH_SETUP.md).

The GrowthZone OAuth proxy in [`supabase/functions/growthzone-auth/`](supabase/functions/growthzone-auth/) and the Swift clients under `Services/GrowthZone/` are retained for reference only and are **not** wired through `AppDependencies`.
